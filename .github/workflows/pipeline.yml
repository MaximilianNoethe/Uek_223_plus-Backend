name: Hello World CI

on: 
  push:

jobs:
 build:
   runs-on: ubuntu-latest

   steps:
   - uses: actions/checkout@v3

   - name: Print Hello World
     run: echo "Hello World"
   - name: Print secret
     run: echo $MY_SECRET
